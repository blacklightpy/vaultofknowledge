- Words allow for more combinations, but takes more strokes
- Buttons allow for less combinations, but with fewer strokes

So buttons are preferable if you've figured out all the strokes one will need to use.